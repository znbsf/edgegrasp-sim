# Candidate011 q=0.40 plan-only observation

The 0.40 rad profile kept all arm poses unchanged and passed the same isolated
three-segment Pilz PTP probe 10 of 10 times. Point counts and trajectory
digests were stable for every segment. No EdgeGrasp motion node was launched;
no trajectory, ExecuteTrajectory goal, or FJT goal was sent.

This was only the execution admission gate. It did not prove contact, lift,
retention, or grasp success. Exact timings, hashes, and cleanup evidence are in
the adjacent JSON.
